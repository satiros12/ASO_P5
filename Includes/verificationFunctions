#Verification Functions.
function Nbt #Number bigger than
{
if [ "$1" -gt "$2" ]
then
		return 0
else
		exit 1
fi 
}

function Ne #Number bigger than
{
if [ "$1" -eq "$2" ]
then
		return 0;
else
		exit 1;
fi 
}
